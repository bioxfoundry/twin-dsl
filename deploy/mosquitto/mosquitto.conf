# Local development broker. It is published on loopback by Compose and carries only
# observation events; production deployments must replace this listener with authenticated TLS.
persistence false
allow_anonymous true
listener 1883 0.0.0.0
